import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.liorpeisakhovsky.bsbapp',
  appName: 'house-efficiency-pro',
  webDir: 'out',
};

export default config;
